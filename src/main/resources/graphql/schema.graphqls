type Query {
    photoById(id: ID): Photo
}

scalar DateTime

type Photo {
    id: ID
    name: String
    createdDate: DateTime
    owner: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}